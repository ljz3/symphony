Review the complete task diff as a fresh principal engineer. Inspect correctness,
security, concurrency, workspace safety, tests, documentation, and the acceptance
evidence. Fix issues you find and revalidate. Transition to `human_review` only
when the branch and pull request satisfy the readiness gate; otherwise transition
to `rework` or `blocked` with a precise reason.
