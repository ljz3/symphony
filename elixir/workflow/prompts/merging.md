Use the repository's `land` skill and squash-merge flow. Monitor conflicts and
required checks until the pull request is merged. Transition to `done` only after
the merge SHA is reachable from the remote default branch.
