# Merge-conflict workpad — {{ task.identifier }}

## Recorded conflict

- [ ] Confirm the recorded task head and target head
- [ ] Merge the target without rebase or history rewrite
- [ ] Resolve only the recorded conflicted paths
- [ ] Commit a clean resolution and any recorded-path-only follow-up
- [ ] Run `full_validation` once for the exact committed source through `symphony_job_run`
- [ ] Push the validated head, then return atomically to automated review

## Resolution evidence
